#include<stdio.h>

int main(){
    int x; //Declara uma variavel inteira com nome x

    scanf("%d", &x); //solicita ao usuario, via teclado, que ele informe um valor inteiro que sera armazenado na variavel x
    printf("%d", x); //imprime na tela o conteudo da variavel inteira x

    return(0);
}
